<%= form_with(model: mention) do |form| %>
  <% if mention.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mention.errors.count, "error") %> prohibited this mention from being saved:</h2>

      <ul>
        <% mention.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :mentioning_report_id, style: "display: block" %>
    <%= form.text_field :mentioning_report_id %>
  </div>

  <div>
    <%= form.label :mentioned_report_id, style: "display: block" %>
    <%= form.text_field :mentioned_report_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
